---
name: trip-plan
description: 从零生成一份可直接执行的旅行行程规划报告。给定「出发地 + 目的地 + 天数 + 出发日期」，自动检索真实攻略数据、按信源质量分级、用规则引擎编排逐日动线、输出含预算与风险的完整报告（Markdown + HTML）。零 LLM 依赖，纯本地运行。支持单目的地与多片区/自驾两种模式（多城市联游、指定日程锚点、含油费过路费估算与逐时时间轴）；内置补查询层（针对闭园维修、预约限流、交通管制、新开试运营、季节限定、票价变动、避坑、船班八个情报方向做缺口检测与定向补查，带相关性闸门与最多两轮迭代）；内置天气层（Open-Meteo 免 Key，逐日预报 + 超窗口时用过往 5 年同期做气候平均，给穿衣/带伞/防晒/大风建议与雨天对调方案）；内置拥挤度层（可针对小长假/暑期逐个景点给出拥挤程度、峰值时段与错峰建议）；内置住宿层（Overpass 免 Key 全球可用 / 高德 POI 国内覆盖最好，输出首选/升级/特色/预算四档与价格档位；**房价无免费开放接口，只给区间、不给伪精确报价**）。路线地图默认是**内联 SVG 两层分面示意图**（每天一格、格内按距离再分簇，每格各自比例尺；零外部依赖、免 Key、离线可渲染、可打印，静态托管不会白屏），可选腾讯地图交互版。跨城长途不上图、视野只框景点、接驳段弱化。报告为**模块化分区版面**（单目的地 9 个模块：概览 / 详细行程 / 城际交通 / 预约与限流 / 备选地点 / 费用 / 路线地图 / 行前与风险 / 信源与核查；叠加天气、近期动向、拥挤度与错峰最多 11 个），长表与明细一律 <details> 折叠，结论入正文。境内走中文攻略源，境外走 Photon/Overpass/OSRM/OpenFreeMap 免 Key 开源栈。零绝对路径硬编码，整个目录可搬到别的电脑；产物为单文件 HTML 与零依赖 SVG 地图，可直接发到手机阅读。适用于：规划一次具体出行、规划自驾多城市联游、评估某个目的地值不值得去、比较多个目的地的行程密度、查询景点错峰时段、查询出行期间天气与装备、核查景点近期是否闭园维修、查看行程路线地图、为已有行程做信源核查、规划境外目的地行程、给出住宿选址与预算档位。
agent_created: true
---

# 行程规划（trip-plan）

把"我想去 X 玩 N 天"变成一份**能照着走**的行程报告。

## 这个 skill 解决什么

手工规划一趟旅行，实际要干四件事，每件都很烦：

1. **到处搜攻略** —— 小红书、马蜂窝、知乎、本地宝，信息散落且互相矛盾
2. **判断信息可不可信** —— 三年前的票价、境外 OTA 的过时信息、张冠李戴的邻市景点
3. **把景点排成不折返的动线** —— 绍兴古城和安昌古镇隔 20 公里，硬排一天就是浪费时间
4. **算清要花多少钱、要预约什么**

这个 skill 把四件事串成一条流水线，全程**不需要调用大模型**，因此：
- 离线可跑，不产生 API 费用
- 结果可复现（同样输入 → 同样输出，没有模型随机性）
- 速度快（一次完整规划约 30 秒）

## 何时使用

**适合：**
- 规划一次具体的出行（"10 月 1-5 日去绍兴"）
- 评估目的地"值不值得去、几天够玩"（跑一次看行程密度就知道了）
- 对比不同天数的行程差异（3 天 vs 5 天分别能玩到什么）
- 核查已有行程的信息可信度（哪些说法来自权威信源、哪些要打问号）

**不适合：**
- 需要实时价格和库存的预订（这个 skill 只给规划，不下单）
- 需要路线导航级精度（给出的是景点顺序和片区，不是逐段交通方案）
- 小众到搜不到资料的野生目的地（检索层拿不到数据就无从规划）

## 两种模式

| 模式 | 脚本 | 适用 |
|---|---|---|
| **单目的地**（默认） | `pipeline.py` | 一个城市玩 N 天，无硬性日程锚点 |
| **多片区 / 自驾** | `places_multicity.py` + `planner_multi.py` + `report_multi.py` + `report_drive.py` | 多城市联游、自驾、有指定日程锚点（"X 日晚出发""Y 日下午到 Z"） |

判断依据：行程里有**任何**"必须某天到某地"的约束，或者跨了两个以上县市，就用多片区模式。
详见 `references/多片区自驾扩展.md`。

## 快速开始

```bash
# 一步到位：检索 → 分级 → 抽取 → 编排 → 出报告
python scripts/pipeline.py \
  --origin 上海 --city 绍兴 \
  --days 5 --start 2026-10-01 \
  --out ./out

# 产物
#   out/graded.json          80 条原始结果的信源分级明细
#   out/places.json          抽取出的景点池（含坐标、票价、时长）
#   out/itinerary.json       逐日行程（含备选、预算）
#   out/报告_上海到绍兴5日.md  人读报告
#   out/报告_上海到绍兴5日.html 可分享版本
```

多片区自驾模式（以「上海→上虞→绍兴→新昌 6 日」为例）：

```bash
# 1. 各片区并行检索（复用同一 research.py）
for c in 绍兴 上虞 新昌; do
  python scripts/research.py search --city $c --start 上海 --out ./$c
  python scripts/research.py grade  --raw ./$c --out ./$c
done

# 2. 建景点池（改 places_multicity.py 的 COORDS/REGIONS/PLACES/MUST_SEE）
python scripts/places_multicity.py

# 3. 编排（改 planner_multi.py 的 DAY_PLAN 指定每天在哪个片区）
python scripts/planner_multi.py

# 4. 出报告
python scripts/report_multi.py    # Markdown
python scripts/report_drive.py    # HTML（含逐时时间轴）
```

只想跑其中某一段，用各脚本的 CLI：

```bash
# 只做检索 + 分级
python scripts/research.py search --query "绍兴 鲁迅故里 门票 预约" --out r1.json
python scripts/research.py grade  --in r1.json --out g1.json

# 只用规则引擎重排行程（换天数试试）
python scripts/planner.py --places places.json --days 4 --start 2026-10-01 --city 绍兴 --out it4.json

# 只重出报告
python scripts/report.py --itinerary itinerary.json --graded graded.json --places places.json \
  --city 绍兴 --origin 上海 --start 2026-10-01 --out 报告.md --html
```

## 核心机制

### 1. 检索层：anysearch

`scripts/research.py` 调用 anysearch 的 JSON-RPC 接口：

```
POST https://api.anysearch.com/mcp
X-Anysearch-Client: skill/2.1.0
```

**Key 处理已全自动**，优先级：显式参数 > 环境变量 `ANYSEARCH_API_KEY` > 进程内存缓存 > 磁盘 `~/.anysearch_key` > 匿名。

首次匿名调用时，服务端可能不返回搜索结果，而是返回一段**自动签发 Key** 的提示：

```
Your account and API key have been automatically generated.
api_key=as_sk_xxxxxxxx
```

`call_anysearch()` 会自动识别这种响应（含 `api_key=` 且不含结果标记 `###`）、
提取 Key、缓存起来并**自动重试一次**。实测：连续 3 次调用只触发 1 次签发。

> 缓存路径可用 `ANYSEARCH_KEY_FILE` 覆盖。磁盘写入失败时静默降级到内存缓存，
> 因此在只读/沙箱环境下依然可用（每次进程启动会重新签发一次）。

`QUERY_PACKS` 定义了一套 10 条查询模板，覆盖一趟出行真正需要的信息面：

| 类别 | 查询意图 |
|---|---|
| 核心景点 | 目的地必去景点、5A/4A 景区 |
| 门票预约 | 票价、是否需要预约、学生/老人优惠 |
| 城际交通 | 出发地到目的地的高铁班次与耗时 |
| 市内交通 | 地铁线路、到达站选择、景区接驳 |
| 美食 | 本地特色菜、老字号 |
| 活动 | 节庆、演出、临展（时效性最强） |
| 避坑 | 踩雷提醒、旺季人流 |

### 2. 信源分级：A / U / C / D 四档

搜索结果不能照单全收，`grade_source()` 按来源域名自动分级：

| 等级 | 判定 | 处理方式 |
|---|---|---|
| **A 权威** | 境内权威媒体、百科、本地宝、政府文旅站、12306 | 直接采信 |
| **U 用户实拍** | 小红书 / 大众点评 / B站 / 美团 / 抖音的**具体笔记页** | 仅作交叉印证，不作票价依据 |
| **C 待核** | 境外 OTA、论坛帖、年份久远的攻略、UGC 泛页 | 采信但标注"需核对" |
| **D 噪声** | 地域无关内容（如查绍兴却返回台南的活动表） | 直接排除 |

> ⚠️ **UGC 必须点名才搜得到**：anysearch 的索引不会主动返回小红书。
> 实测默认 10 组通用词检索 120 条，小红书域仅命中 1 条；追加
> `site:xiaohongshu.com {城市} 攻略` 等定向查询后，408 条中命中 8 条。
> 因此 `build_queries()` **默认开启 UGC 定向包**（CLI 可 `--no-ugc` 关闭）。
>
> **U 与 C 的边界**：`/explore/<id>`、`/discovery/item/<id>` 是真实笔记页（判 U）；
> 裸 `/explore`、`/explore?language=xx`、`/user/profile/<id>` 是列表页或主页（判 C，仅作线索）。

分级结果会写进报告的"信源质量说明"章节，并附上完整的 A 级与 U 级信源清单（含可点击链接），让用户能自己复核。

### 3. 抽取层：规则引擎识别景点

`scripts/extract.py` 从分级后的文本里抽出景点名。难点在于过滤句子碎片——检索结果里常出现"除了魯迅故里和沈園""行程涵盖鲁迅故里"这类表达，朴素的正则会抽出半个句子。

过滤策略（四层）：
1. `name.endswith(后缀)` 严格判定，不做子串匹配
2. `EXCLUDE_PATTERNS` 排除句式碎片
3. 繁→简转换表（`T2S`）统一变体
4. `KNOWN_SPOTS` 白名单（34 个绍兴地点）标记高置信度，命中即 `confidence: high`

### 4. 编排层：规则引擎排行程（核心）

`scripts/planner.py` 是这个 skill 最有价值的部分。核心难点：**把一堆景点排成不折返、不浪费、招牌不遗漏的逐日动线**。

经过多轮调优，最终采用**四阶段算法**：

**阶段 0 · 名片保底**
城市级"来都来了"点位（`MUST_SEE`）无条件入选。这些点可能在检索文本里缺 4A 标注导致评分被低估（例如兰亭），不能靠打分决定去留。同时按地理距离做贪心聚类分组，相邻名片同组 → 同一天，避免"柯岩 + 五泄"这种跨 50 公里的硬排。

**阶段 1 · 片区保底配额**
按"综合价值密度"给每个片区预留天数。密度 = 前 N 个高点的分数之和 ÷ 所需天数 − 离城距离 × 0.28。

> **关键坑**：离城距离必须从**市中心锚点**（`CITY_CENTER`）量，不能从片区自身中心量——否则永远是 0，"远郊"语义完全失效。这个 bug 排查花了很久。

**阶段 2 · 配额执行**
密度高的片区先挑天，优先填"已有本片区点的天"（便于成组），其次填空天。

**阶段 3 · 全局补位**
剩余容量按全局分数降序填充，每天受 `PER_DAY`（默认 4）+ `MAX_DAY_SPAN_KM`（默认 18km）双重约束。装不下的进备选。

**远郊名片降级**
离城 > `FAR_SIGNATURE_KM`（默认 45km）的名片不占主行程天数，转入"一日游延伸选项"。单程 1.5 小时以上，当天往返会挤掉市区深度体验——这是产品决策，不是算法限制。跑 6 天时它会自动回来填补空档。

**去重**
- 父子景点合并（`PARENT_CHILD`）：鲁迅故里已在列时，三味书屋/百草园不再单独占位
- 名称变体归并（`CORE_ALIAS`）："绍兴柯岩" / "柯岩风景区"只留一条
- 行程外地点剔除（`OFFROUTE_EXACT`）：从交通查询泄漏进来的"上海虹桥""西湖"等

### 5. 报告层：模块化版面 + 结构性章节

**版面形态（先生 2026-09-15 指示）**：报告**不做"一页到底"的长文档流**，
改为 **模块分区 + 顶部粘性导航**。报告 Markdown 用 `<!--MODULE:名称:图标:简介-->`
切分，`md2html.py` 解析后渲染成可点击切换的模块。

**模块数 6 → 10**（先生 2026-09-17 指示：**「6 个项目栏太少了，我觉得可以多几个栏目」**）。
福州实例的 **10 个模块**：

| 模块 | 归并了哪些章节 | 新增 |
|---|---|---|
| **概览** | 一图速览 + 四天一句话 + 天气速览行 + 出发前必做提醒 | |
| **详细行程** | 逐日行程（每天一张表 + 路段 + 餐饮） | |
| **天气与装备** | 逐日天气表 + 穿衣 / 装备 / 风险 / 行程影响 | ✨ 新增 |
| **近期动向** | 运维闭园 / 台风 / 新开试运营 / 季节 / 票价 + 补查询覆盖自检 | ✨ 新增 |
| **预约与限流** | 必订三件套 / 门票预约 / 停车 / 交通管制 / 现场应对三步 | ✨ 新增 |
| **路线地图** | 内嵌 SVG 分面示意图 + 看图要点 | |
| **费用** | 城际交通 + 门票 + **费用区间** + 住宿 + 手信 | |
| **拥挤度与错峰** | 逐个研判（折叠）/ 总原则 | |
| **备选地点** | 主要备选（表）+ 次要备选（文字） | |
| **行前与风险** | 风险登记 + 7 日清单 + 信源（折叠） | ✨ 拆出单列 |

**拆并原则**：把原来挤在「费用与行前」里的**风险 / 清单 / 信源**拆成独立模块；
新增的三个模块（天气、动向、预约）都是**「出门前要单独做动作」**的类型 ——
它们不该埋在费用章节里。

**模块切分规则**：
- 一行 `<!--MODULE:名称:图标:简介-->` 开启一个模块，到下一个标记为止。
- 图标用一个字符即可（`◎ ▤ ￥ ◐ ◇ !`）；emoji 也可用（如 `🗺`），现代浏览器表现正常。
- 模块内首个 `## 标题` 会被剥掉 —— **模块名已承担该职责**，避免"模块名 / 章节名"重影。
- 模块内的次级标题从 `###`（h3）起用。
- **地图要独立成模块**，不要塞进概览。地图模块正文里放一行 `<!--MAP-->`
  占位符，`md2html.py --map-itin itinerary.json` 会自动把它替换为内嵌地图。
  **拿不到地图时**（没传 `--map-itin` / 行程缺坐标 / 地图生成失败），
  `md2html.py` 会清掉占位符，并在模块因此变空时**整体不输出该模块** ——
  不会留一个点开是空白的标签页。

**模块集是条件性的，不是固定的 10 个。** 实际输出取决于跑了哪些层：

| 路径 | 典型模块数 | 模块集 |
|---|---|---|
| **单目的地一键**（`pipeline.py` → `report.py`） | **9** | 概览 / 详细行程 / 城际交通 / 预约与限流 / 备选地点 / 费用 / **路线地图** / 行前与风险 / 信源与核查 |
| **多片区自驾**（`report_multi.py`） | 10~11 | 上面 + **天气与装备** + **拥挤度与错峰**（+ 近期动向） |
| **境外自驾**（冰岛实例） | 10 | 上面 + 天气与装备 + 近期动向 + 拥挤度与错峰 |

上表「10 个模块」是**上限形态**，不是硬性要求。**别为了凑数而生成空模块** ——
没有天气数据就不放天气模块，没跑补查询就不放近期动向模块。

⚠ **`report.py` 的叙述文字是「上海 → 绍兴」调过的**（车次、到站说明、优惠政策、
基调描述都是写死的），换目的地必须替换那些段落；`report_multi.py` /
`report_html.py` / `report_drive.py` 同理（数据表整段替换）。结构层通用，文案层不通用。

**报告层两个坑（2026-09-17 踩过，已修，但写新报告时要知道）**：

1. **说明性注释会被误当成真模块。**
   如果报告或模板里写了「用 `<!--MODULE:名称:图标:简介-->` 切模块」这样的**说明文字**，
   切分正则会把它识别成一个真模块，于是报告顶部凭空多出一个叫"名称"的空标签页。
   已修法：`_strip_comments()` 在切分前剥掉普通 HTML 注释，只保留 `<!--MODULE:...-->`
   与 `<!--MAP-->`。**自己写模板时仍建议别在注释里直接写出完整标记。**

2. **`md2html.convert()` 的 `map_itin` 要的是已解析的 dict，不是路径。**
   传路径字符串进去，`build_payload()` 会在 `'str' object has no attribute 'get'` 处失败，
   而该异常被 `convert()` **内部 try 吞掉**，只打印一行
   `⚠ 地图片段生成失败，报告将不含地图` —— 表现为「报告正常生成，就是没地图」，
   极难定位。正确写法是调用方先 `json.load()`。

#### 内嵌地图（单文件交付，先生 2026-09-15 指示）

先生要求「**把地图与报告合并**」——产出单文件 HTML，不再有独立的 `map.html`。

```bash
# 默认：内嵌 SVG 分面示意图（零外部依赖，静态托管也能看）
python md2html.py --md 报告.md --out 报告.html \
    --map-itin itinerary.json --map-station 福州站
```

`--map-itin` 会替代模块正文里的 `<!--MAP-->` 占位符。
**默认 `--map-mode svg`**；想要腾讯交互版加 `--map-mode tmap`，
不要地图用 `--map-mode none`。详见第 6 节「地图层」。

**⚠ 三个致命前端陷阱，改这块代码前必读**：

1. **模块 JSON 里的 `</script>` 会让整页 JS 崩溃**。内嵌 HTML 片段天然含
   `</script>`，写进 `<script>const MODULES = [...]</script>` 时浏览器会提前闭合
   —— **不管它是否在 JS 字符串里**。必须 `json.dumps(...).replace("</", "<\\/")`。
   **自检**：
   ```python
   m = re.search(r"const MODULES = (\[.*?\]);\n", html, re.S)
   json.loads(m.group(1))          # 能解析 = 没被提前闭合
   ```
2. **`innerHTML` 注入的 `<script>` 不执行**，需用 `DOMParser` 重建 script 节点
   （`src` 型要 `s.async=false` 保序）。切 Tab 时还要调 `window.__rebuildMap__()`
   重算视野（容器初始 `display:none`，尺寸为 0 时 `fitBounds` 算错）。
   腾讯 SDK 异步加载需轮询 `boot()`。
3. **SVG 相关的三个可移植性坑**（`height="auto"` / `paint-order` / 圆点 z 序），
   详见第 6 节。症状分别是：整图空白 / 标签批量消失 / 编号被盖住。

> 注意陷阱 1 与 2 只对**腾讯地图版**成立；SVG 版没有 `<script>`、也没有
> `fitBounds`，天然规避。但 `replace("</", "<\\/")` 仍必须保留 ——
> 它保护的是**整个模块 JSON**，任何模块正文里出现 `</` 都有风险。

**独立 `map.html` 仍保留**（`map_html.py` 单独跑），供在 WorkBuddy 预览面板里
平移缩放查看；但报告不再依赖它，也不能把它直接作为交付文件。

#### 费用区间（先生 2026-09-15 指示）

先生原话：**「重新计算费用，我觉得费用有问题，你直接列出『预计费用合计区间』
给出最小费用和最大费用」**。

**铁律：不输出单一费用数字，只输出区间。** 拆两层：

| 层 | 含义 | 项 |
|---|---|---|
| 每车固定（含 1 间房） | 不随人数变化 | 租车、全险、油费、过路费/停车、住宿 |
| 每人 | 随人数线性增长 | 高铁往返、门票、餐饮 |

合计 = `固定下限 + 每人下限×人数` ~ `固定上限 + 每人上限×人数`。
输出「按同行人数」表（1/2/3/4 人）+ 两张明细表；**手信单独列，不并入主区间**。


#### ⚠ 境外自驾：油费口径已变（2026-09-17 实证修正）

**冰岛（以及后续可能出现同类立法的国家）不能用"油价 × 油耗"单算油费了。**

- 冰岛国会 2025-12-18 通过《公里收费法》，**2026-01-01 起里程税取代燃油税**：
  3.5 吨以下车辆 **6.95 ISK/km**（分 29 个重量级距，越重越贵）；**2027 年上调 15.9% → 约 8.05 ISK/km**。
  官方数据源：**island.is/en/kilometer-fee**（A 级，可直连）。
- 燃油税移除后油枪**降价约三分之一**（首都 95 号汽油 279~308 → 183~212 ISK/L）。
- **净效果几乎打平**：政府的设计目标就是"让一辆典型汽油车在新旧制度下的总负担相当"。
  实测同一趟 2,074 km 行程：旧口径（油价 300 ISK/L）≈ ¥2,764；
  新口径（油价 200 ISK/L + 里程税）≈ ¥2,642。**总预算不用改，但构成必须拆开写。**

**因此境外自驾的费用表必须拆成三行**：油费 / **里程税（单列）** / 隧道过路与停车。

**必须写进报告的避坑点**：
> **"Unlimited Mileage（不限里程）" ≠ 已含公里税。**
前者只是"租车公司不限你开多少、不因里程罚你租金"；后者是政府按实际公里向车主收的
道路使用费，无论套餐是否不限里程都要算，通常在**还车时**另收。各公司分摊方式无统一标准
（日租上调 / 单独每公里附加费 / 包进全包价），**订车前必须逐条问清**：
是否已含、每公里多少 ISK、有无封顶、有无"含 N 公里"套餐。

**成本口径自检**：若沿用旧油价而不加里程税，会**漏算约 ¥800/车**——这类"看起来差不多对"
的错漏最难发现，务必在自检表里单列一行。

#### 报告篇幅四条硬规范（先生 2026-09-15 指示）

| 规范 | 做法 |
|---|---|
| **不写元信息** | 正文禁止出现"本报告由 X 引擎生成""零 LLM 依赖"等自我说明 |
| **信源必须折叠** | A 级清单用 `<details><summary>` 折叠，详见 `references/多片区自驾扩展.md` 陷阱 6 |
| **备选必须分层** | 主要备选 ≤8 个成表；其余按片区归并成一句话文字列表 |
| **明细必须折叠** | 逐个景点的研判明细（4.1）用 `<details>` 折叠，正文只留结论性的总原则（4.2）与替换矩阵（4.3）——**结论入正文，论据进折叠** |
| **自检表必须折叠** | 「补查询覆盖自检」的 23 行明细表用 `<details>` 折叠，正文只留**结论**：共检测多少项 + **哪几个方向信息偏薄**（先生 2026-09-17 指示） |

**可折叠的四类内容**（先生 2026-09-17 明确）：

| 类别 | 处置 |
|---|---|
| 元信息（生成方式、引擎、检索条数） | **直接删**，不折叠 —— 折叠了也是噪声 |
| 长信源清单 | `<details>`；A 级信源清单默认收起 |
| 逐条明细（逐个景点研判、逐片区逐方向自检） | `<details>`，正文只留结论 |
| 低频参考数据 | `<details>` |

**不可折叠**：一图速览、逐日动线、预算、错峰总原则 —— 这些是正文的骨架。

实测：宜兴 4 日报告按此规范从 30,467 字符压到 13,924 字符（**46%**），
且保留了全部表格与信息量。

> **通用原则：报告要给结论，不要给推导过程。** 凡是"逐个 X 的详细研判"这类
> 明细，读者多数时候只想扫一眼结论；真正要细看时才展开。因此：
> 结论表 / 总原则 / 替换矩阵留正文，逐个明细一律 `<details>` 折叠。

### 6. 地图层：行程可视化（`map_html.py`）

先生 2026-09-15 新增要求：**最好加一个可视化地图，方便看具体景点间的路线和距离**。

**两种形态，默认走 SVG**：

| 形态 | 触发 | 依赖 | 用途 |
|---|---|---|---|
| **内联 SVG 分面示意图**（默认） | `--map-mode svg` | **零外部依赖** | 报告正文内嵌；静态托管 / 离线 / 打印 / 发邮件都能看 |
| 腾讯地图交互版（可选） | `--map-mode tmap`，或单独跑 `map_html.py` 出 `map.html` | 需联网 + 本地服务端口 | 想平移缩放、看底图时用 |

> **为什么默认改成 SVG（2026-09-17）**：先生反馈「地图无法显示」。
> 根因是腾讯免 Key 代理的 `serviceHost` 依赖 `__WB_HTTP_PORT__` /
> `__WB_TMAP_SECRET__` **运行时占位符注入**，而报告是**静态 HTML 交付**
> （`present_files` / 本地文件），**没有任何运行时去替换它** →
> SDK 去请求 `http://127.0.0.1:__WB_HTTP_PORT__/...` → 必然空白。
> 先生建议「用截图或者矢量图等形式替代」—— 采纳，用矢量图（SVG）。

#### 内联 SVG 分面示意图（默认形态）

**两层分面**是这张图的核心：

1. **第 1 层：一天一格**。每天一个方框，各自独立比例尺。
2. **第 2 层：格内按距离再分簇**（单链聚类，25 km 阈值）→ 子格，**每簇再各自一个比例尺**。

**为什么必须这么做**：若把四天压进同一幅等距投影，城区几个相距 3~5 km
的景点会被 125 km 的跨海长途拉平成一团 —— 这正是先生反对的
「篇幅被长途吃掉」在版面上的翻版。若只做第 1 层（一天一格），
**一天内同时跑两个相距 125 km 的片区仍然会坏**（福州样本的 10/04：
上午平潭南线 15 km、下午回福州仓山），所以还要第 2 层。
**跨簇的长途不画线，只在格底用文字列出里程与车程。**

函数清单（`map_html.py`）：

| 函数 | 职责 |
|---|---|
| `_cluster(nodes, thr_km)` | union-find 单链聚类，把一天的点按距离切簇 |
| `_proj(...)` | 等距投影到子格矩形；**上下留白刻意不对称**（`pad_t=24, pad_b=48`），给底部比例尺让位 |
| `_put_label(...)` | 10 方位标签避让 + **白底圆角矩形垫底** |
| `_draw_subpanel(...)` | 画一个子格：段 → 里程标签 → 圆点 → 站点名 → 名称 |
| `_draw_day(...)` | 格内聚类 + **子格高度按簇跨度加权**（下限 `0.35×最大跨度`），单点簇不白占半格 |
| `render_svg(...)` | 2 列 × N 行布局 + 底部「跨天转移」条 |
| `render_svg_block(...)` | 带外框与图注，供报告内嵌 |

**⚠ 三个可移植性坑（踩过，务必记住）**：

1. **`height="auto"` 是非法 SVG 属性**。浏览器容错，但 `rsvg-convert` /
   图片导出会**整张空白**。必须写数值 `width`/`height`，
   响应式交给 `style="width:100%;height:auto"`。
2. **`paint-order:stroke` 不可移植**。librsvg **不支持**该属性，
   会「先填充后描边」，把深色文字描成白字 → **整批标签消失**。
   正确做法：在文字**下方**垫一个白色圆角矩形，任何渲染器都正确。
3. **圆点必须压在里程标签之上**。绘制顺序固定为
   **段 → 里程标签 → 圆点 → 站点名 → 景点名**；并先用 `placed` 登记所有
   标记占位，再给标签找空位。短段（屏幕长度 < 52px）强制垂直偏移 24px，
   否则标签中点和圆点必然重叠。

**自适应密度**：两点屏幕距离 < 30px 时整格缩小圆点与字号
（`R=7.6 / FS=9.2`，默认 `10.5 / 11.0`）。

#### 合规红线（务必遵守，见 `geo-map-compliance-guard` skill）

- **只允许**腾讯地图 / 高德 / 百度 / 天地图。**禁止** Google Maps、Apple Maps、
  Mapbox、Leaflet + OpenStreetMap、境外版 Bing、直接引境外瓦片。
- **绝不内置任何 Key**。高德/百度的公开接口需自行申请 Key，不适合开箱即用。

#### 为什么交互版走腾讯地图代理模式（可选形态）

高德与百度的 REST/JS 接口都**需要 Key**（实测：高德返回 `INVALID_USER_KEY`、
百度返回 `APP不存在，AK有误`）。腾讯地图 GL JS 有**官方免 Key 代理模式**
（`_TMapSecurityConfig`），由 WorkBuddy 后端持 Key、前端零暴露 —— 开箱即用，
是当前环境下唯一"零配置可用"的合规方案。

```html
<!-- 必须在 SDK 之前，且占位符原样保留 -->
<script>
  window._TMapSecurityConfig = {
    serviceHost: 'http://127.0.0.1:__WB_HTTP_PORT__/_TMapService/_wbt/__WB_TMAP_SECRET__',
  };
</script>
<script src="https://map.qq.com/api/gljs?v=1.exp"></script>
```

#### 用法

```bash
# ① 报告内嵌 SVG 示意图（默认，无需任何额外步骤）
python md2html.py --md 报告.md --out 报告.html \
    --map-itin itinerary.json --map-station 福州站        # map-mode 默认 svg

# ② 想要腾讯地图交互版 → 单独出 map.html（不要嵌进报告）
python map_html.py --itin itinerary.json --out map.html
python md2html.py --md 报告.md --out 报告.html --map-mode tmap \
    --map-itin itinerary.json --map-station 福州站
```

`--map-mode` 三态：`svg`（默认）/ `tmap`（腾讯交互版）/ `none`（不要地图）。

> **注意**：`tmap` 模式产出的 HTML **只在 WorkBuddy 预览面板里能正常显示**；
> 一旦作为静态文件分享出去（含 `present_files` 托管、离线打开），
> 占位符无人替换 → 地图空白。**要交付给别人看，一律用 `svg`**。

**前置条件**：`itinerary.json` 的每个景点必须带 **GCJ-02** 经纬度。
多片区 planner 已输出 `lng` / `lat` / `coord_sys` / `anchors`（车站+城区）字段。

#### 地图要素

| 要素 | 实现 |
|---|---|
| 逐日路线 | `TMap.MultiPolyline`，**按天分色**（`DAY_COLORS`） |
| 主景点 | `TMap.MultiMarker` + 内联 SVG 水滴图标（**不引用官方 demo 图**） |
| 行程锚点 | 车站单独样式（深色 + 列车图标），首末两天连到车站 |
| 主要备选 | 半透明小圆点，**默认不显示**，点「显示备选」才进图层 |
| 距离标签 | 相邻两点**直线距离** + **车程估算**（绕行系数 1.35 / 均速 45 km/h） |
| 侧栏 | 逐日卡片（点开看当天点位 + 逐段距离），点击可折叠 |
| 段类型 | `inter`（景点↔景点，正常绘制）／ `access`（车站接驳，弱化 + 打「接驳」标签） |
| 视野 | `fitBounds` **默认只用景点坐标** |

#### 地图做减法（先生 2026-09-17 指示）

先生原话：**「把这种跨城的长途旅行就不要显示在图上，比如上海到福州，
只要显示景点之间的交通情况就好，不需要把所有东西都放在地图上」**。

三条规则（**交互版与 SVG 版都必须遵守**）：

| 规则 | 做法 | 原因 |
|---|---|---|
| **城际段不上图** | 出发地 ↔ 目的地不画线、不放标记；改为侧栏 `.crossbox` / SVG 底部文字 | 近 800 km 的城际线会把几十公里的市内动线**压成一个点**，地图失去判断「今天会不会绕路」的作用 |
| **视野默认只框景点** | `fitBounds` 只用景点坐标；车站锚点与备选点要手动开按钮才参与 | 一个离群点就能把视野撑大三四倍 |
| **接驳段弱化** | 车站 ↔ 首/末景点 的段加 `.access` 类（降透明度）+ 「接驳」标签；分列 `leg_inter_km` / `leg_access_km` | 它是必须走的，但不是需要你判断「要不要调整」的段 |

配套给出三个视图按钮：**仅景点 / 含站点锚点 / 显示备选**（默认：仅景点）。
「当日车程」也拆成 **景点之间 X km ＋ 接驳 Y km**，不再混成一个数。

**SVG 版把「减法」推到第三层**（这就是两层分面的来源）：

| 层 | 做法 |
|---|---|
| ① 城际段 | 完全不上图，只以文字交代 |
| ② 一天一格 | 每格**各自比例尺**，不让长途压掉短途 |
| ③ 格内分簇 | 一天内若有两个相距很远的片区，**再拆子格**，跨簇长途只列文字 |

报告正文里同步写「**看图的四个要点**」，把这三点减法讲透 ——
否则用户会以为图不完整。

#### 坑（以下为**交互版**；SVG 版的坑见上面「三个可移植性坑」）

- **`_TMapSecurityConfig` 必须在 SDK `<script>` 之前**，否则地图空白。
- **⚠ 最致命的坑：占位符不会被替换 → 交付即空白**。`serviceHost` 里的
  `__WB_HTTP_PORT__` / `__WB_TMAP_SECRET__` 只有**运行时注入**才有效。
  静态 HTML（present_files 托管、发邮件、拷给别人）没有运行时 →
  必然空白。自检命令：
  ```bash
  grep -c "__WB_HTTP_PORT__" 报告.html   # 交付文件里应为 0
  ```
- **SDK URL 不能带 `?key=`**，也不能额外调 `TMap.setConfig()`（两者同用会白屏）。
- **不要用 `mapStyleId`** —— 自定义样式需在控制台单独开通，默认 Key 不支持，误用会变灰。
- **容器必须有固定高度**（`height:100vh` 或像素值），否则不渲染。
- **不要手工 `fetch(serviceHost + '&path=...')`** —— 路由/POI 搜索要走
  `TMap.service.Driving` / `TMap.service.Search`，SDK 内部自动走代理。
- 车程是**估算值**（直线 × 1.35 ÷ 45 km/h），非导航数据，报告里要写明"以导航为准"。
- **不要把车站/备选点算进默认 `fitBounds`** —— 见上「地图做减法」。
- 城际说明的三种传法（优先级从高到低）：
  1. `map_html.py --cross "上海虹桥 → 福州站 · 高铁 G1653 08:15-11:55 · 二等座 ¥377.5"`
  2. `itinerary.json` 的 **`"cross"` 字段**（推荐 —— 属于行程数据，重跑 planner 不丢）
  3. `md2html.py --map-cross "..."`（只影响报告内嵌图）
  只进侧栏/说明框，**不绘图**。
- **嵌入版也必须渲染城际说明框**（2026-09-17 修）。`render_html()`（整页版）有侧栏
  `.crossbox`，但 `render_embed()`（报告内嵌版）**原本没有** ——
  结果是城际段在整页图里是「文字交代」，在报告里变成「凭空消失」。
  已补 `.mapwrap .crossbox` + `<div id="__MAPID__cross">` + `D.cross` 运行时注入。
  **改地图时两个模板都要改**，这是本文件最容易漏的地方。

### 7. 补查询层：情报缺口驱动（`gapfill.py`）

先生 2026-09-17 指示：**「要学习它的补查询信息源和补查询动作，能体现出不少近期的景点动向」**。

常规检索答的是**存量问题**（有哪些景点、门票多少、怎么去），
答不了**增量问题**（现在还开着吗、国庆要不要预约、最近有没有维修）。
后者零命中也毫不显眼 —— 报告里少一段话你看不出来，但它可能就是
「白跑一趟」与「顺利玩到」的差别。

所以触发依据不是「章节字数够不够」，而是**「情报方向的命中次数 < 阈值」**。

八个方向（`GAP_PACKS`）：
运维动向（闭园/维修/施工）、预约限流、交通管制与停车、新开试运营、
季节限定、票价变动、避坑提醒、船班航线（**有海岛才评估**，`when_any` 门控）。

**三道闸门**（缺一不可，详见 `references/补查询与质量自检.md`）：

1. **URL 去重**
2. **相关性过滤**（`is_relevant`）—— 域名黑名单 + **标题/摘要必须出现目的地名或别名**。
   > 这是最关键的一步。补查询极易召回到「邻市/邻省」内容，而它们因为是政府站/官媒域名
   > 反而拿到 A 级。**实测 147 条新增里 84 条（57%）被这道闸门丢弃。**
   > 别名用 `--alias` 传（如 平潭 → `--alias 福州`；永泰 → `--alias 福州 云顶 青云山 嵩口`）。
3. **最多 2 轮迭代**；2 轮后仍不达标 → **如实写「信息有限」并在报告里显式列出**，不编造。

```bash
python scripts/gapfill.py check --graded ./ly/graded.json --city 福州          # 只判缺口，不联网
python scripts/gapfill.py run   --graded ./ly/graded.json --city 福州 --out ./ly   # 补一轮
python scripts/gapfill.py fill  --graded ./yt/graded.json --city 永泰 --alias 福州 --out ./yt
python scripts/gapfill.py prune --graded ./ly/graded.json --city 福州          # 事后清理无关条目
```

产出：`graded.json` 原地合并（新条目带 `source: "gapfill"` / `gap_key`）+ `gaps.json` 机器可读自检。

**报告落法**：结论入正文（按「运维 / 台风 / 新开 / 季节」分组，每条写
「发生了什么 → 对我这趟的影响 → **怎么办**」三段，第三段必须给具体替代方案），
信源进 `<details>`；再加一张「补查询覆盖自检」表，`still_gap` 必须显式写出来。

### 8. 天气层：逐日天气 + 装备建议（`weather.py`）

先生 2026-09-17 指示：**「要不要找一个端口查一下旅行期间的天气，并给予相关穿衣带伞等建议」**。

数据源 **Open-Meteo**（`api.open-meteo.com`）：**免费、免 Key、全球覆盖**。
这是 skill 里唯一一套**境内境外通用**的数据源。

**双轨取数**（行程通常提前 1~3 个月规划，那时没有预报）：

| 模式 | 条件 | 接口 |
|---|---|---|
| `forecast` | 整段在今日 ~ +15 天内 | `/v1/forecast` |
| `mixed` | 只有前若干天在窗口内 | forecast + archive 拼接 |
| `normal` | 整段超出窗口 | `/v1/archive` 取**过往 5 年同期**求平均 |

**诚实性要求**：`normal` / `mixed` 模式下必须标注「气候平均 · 非预报」，
逐日标 `≈`。**气候平均只能定「带什么衣服」，不能定「哪天下雨」。**

建议引擎（`advise()`）输出四类：**穿衣**（含昼夜温差提醒）／**装备**（带伞、防晒、防风）／
**额外风险**（沿海 7-10 月台风季、大风、高温、低温）／**对行程的影响**（雨天对调方案、日落时间）。

```bash
python scripts/weather.py --start 2026-10-02 --end 2026-10-05 \
    --point 福州:119.3034,26.0745 --city 福州 --out ./weather_ly.json
python scripts/weather.py --itinerary ./itinerary.json --out ./weather.json   # 自动取每日动线重心
python scripts/weather.py --start 2026-12-20 --end 2026-12-23 --point 福州:119.3,26.07 --mode normal
```

### 9. 境外双轨：信源与地图

先生 2026-09-17 指示：**「地图和信息源方面，可以找找国外的开源媒体和地图，万一要去国外旅游呢」**。
已落地脚本：**`scripts/overseas_research.py`**（四条子命令 `geocode` / `pool` / `route` / `all`）。
完整方案见 `references/境外行程扩展.md`。

| 轨道 | 底图 | 地理编码 / POI | 信源 |
|---|---|---|---|
| **境内** | 腾讯 GL JS（免 Key 代理）/ 内联 SVG | 腾讯 / 高德 | 中文攻略、本地宝、政府站、小红书 |
| **境外** | **MapLibre GL + OpenFreeMap**（免 Key 可商用） | **Photon + Overpass** | OSM 属性 + `wikidata`/`wikipedia` 标签 + anysearch 检索攻略 |

#### ⚠ 本环境的网络出口白名单（实测结论，别再试错）

| 服务 | 状态 | 处置 |
|---|---|---|
| `overpass-api.de` / `overpass.private.coffee` | ✅ 通 | POI 主力（脚本自动主备切换） |
| `router.project-osrm.org` | ✅ 通 | **真实道路距离/时长**，比境内「直线 ×1.35」估算更准 |
| `photon.komoot.io` | ✅ 通 | 地理编码（**返回 bbox**，比 Nominatim 更适合本用途） |
| `tiles.openfreemap.org` / `demotiles.maplibre.org` / `basemaps.cartocdn.com` | ✅ 通 | 底图瓦片 |
| `api.open-meteo.com` | ✅ 通 | 天气（**境内境外通用**） |
| `open.er-api.com` / `api.exchangerate-api.com` | ✅ 通 | **实时汇率**（免 Key）。住宿层用它把 ISK/USD 折 ¥ |
| `visiticeland.com` / `safetravel.is` / `ferdamalastofa.is` | ✅ 通 | 冰岛旅游局与安全提示（可检索/可抓页面） |
| `farmholidays.is` / `hostel.is` / `nat.is` | ✅ 通 | 冰岛住宿官方名录（**房价唯一可核的免费来源**） |
| `vedur.is` / `api.vedur.is`（冰岛气象局） | ❌ 不可达 | 冰岛官方气象数据这条线是断的，天气只能用 open-meteo |
| `road.is` / `api.vegagerdin.is`（冰岛公路局路况） | ❌ 不可达 | **路况只能靠检索**，不能做 API 直连 |
| `api.straeto.is`（冰岛公交） | ⚠ 400 CSRF 拦截 | 无实际用途（自驾用不到） |
| **整个 Wikimedia**（`en.wikipedia.org` / `en.wikivoyage.org` / `commons`） | ❌ **被墙** | **原方案的 Wikivoyage 主力信源作废** → 改用 anysearch 检索攻略 |
| `nominatim.openstreetmap.org` / `.de` | ❌ 被墙 | **改用 Photon** |
| `tile.openstreetmap.org` / `opentopomap` / `stadiamaps` | ❌ 被墙 | 改用 OpenFreeMap / CARTO |
| `overpass.kumi.systems` | ⚠ **可达但易 504** | **纠正早前误判**：它没被墙，只对大查询超时。
已作为 Overpass 主备之一写进 `lodging.py`；重查询优先走 `overpass-api.de` |
| `overpass.private.coffee` | ⚠ 时通时断 | 不作主备，仅记录 |
| `api.geoapify.com` | ❌ 被墙 | — |

> **教训**：设计境外方案时**先做可达性探测**，再写代码。原方案（Wikivoyage + Nominatim）
> 看起来最正统，在本环境里**两个都不可达**，等于零。

#### 境外 POI = 「境外的信源分级」

OSM 没有 4A/5A 字段，用 **`wikidata` / `wikipedia` 标签**当闸门
（有独立百科条目 ≈ "值得一提"）。实测京都 bbox 拿到 **3351 个 POI，其中 577 个带 `wikidata`**。

打分三层（`score_poi()`）：
1. **`FAMILY_SCORE`** 按标签族给基础分（castle/heritage 9.0 → viewpoint 5.0 → artwork 3.0）
2. **`heritage` 标签**加权
3. **`DERIVED_HINTS` 压制派生对象**：名称含 `の眺め / から見た / Viewpoint of / 駐車場 /
   parking / バス停` 者为附属物（实测「二条城」曾误配
   「西から見た二条城東南隅櫓の眺め」，就是这么来的）

查询用 **6 个标签族联合**（只查 `tourism=*` 会漏掉古迹、教堂、山峰、瀑布）。

#### 坑

- **Photon 候选选择**：查 `Kyoto` 首条常是**同名行政区**（bbox 仅 4 km），
  会让清水寺/金阁寺全落在框外、整池降级为小众博物馆。
  用 `nest` 标记 + **bbox 面积最大者** + `PREFER_KIND`（city/town/municipality）挑顶层城市。
  同名地名（`Florence` → 美国南卡、`Barcelona` → 委内瑞拉）仍可能选错，
  兜底用 `--pick <display 子串>` 手工指定。
- **Overpass 要用 2×2 网格拆分**（`split=2`）降压力，否则大 bbox 易超时。
- **不要用 `tile.openstreetmap.org`**。OSM 的**数据**是自由的，OSMF 的**瓦片服务器**不是 ——
  Tile Usage Policy 明确禁止需要批量/实质流量的产品，违规无预警封禁。
  一律改用 OpenFreeMap / CARTO / 自托管。
- **`to_places()` 输出与国内 `places.json` 同构** —— 可直接喂给 `planner_multi.py`，
  这是双轨能共享编排算法的关键。
- **跨国行程分段处理**：中国段走境内轨，境外段走境外轨，不要用一张图覆盖。

### 10. 拥挤度层：小长假 / 暑期专项研判（`crowd_research.py`）

先生 2026-09-15 新增要求：**更多获取小红书、大众点评、携程、飞猪、去哪儿、游侠客
等公开信源的最新信息**，针对小长假与暑期游给出**景点拥挤程度与错峰建议**。

由于拥挤度是**景点维度**问题（而 `research.py` 是城市维度、用于建景点池），
需单开一层：

```bash
# 默认并发 4，逐条落盘，中断可续
python crowd_research.py --spots "善卷洞,宜兴竹海,窑湖小镇" --out ./crowd

# 景点多、额度紧：只跑 3 条通用拥挤意图，跳过 6 平台 site: 定向
python crowd_research.py --spots "A,B,C,..." --out ./crowd --only-crowd

# 中断后续跑（读 raw.json 跳过已完成的 query）
python crowd_research.py --spots "A,B,C,..." --out ./crowd --resume
```

- **查询构成**：每景点 3 条拥挤度通用查询（"人多吗/几点去人少/国庆堵吗"）
  + 6 个平台 × 2 条 `site:` 定向 = **15 条/景点**
- **实测命中（宜兴 8 景点 / 960 条）**：大众点评 49、小红书 21、游侠客 21、
  飞猪 16、去哪儿 16、携程 14（A/U 级）。**比默认查询包的 UGC 命中率高一个量级**
- **产出**：`crowd/graded.json`，带 `platform` / `spot` / `kind` 字段

> **性能坑（2026-09-17 修复）**：14 景点 × 15 = 210 组查询串行需 **8 分钟以上**，
> 且原实现在**全部跑完才写 `raw.json`** —— 中途超时/被杀就一条不剩。
> 现已改为 **`--workers 4` 并发 + 每条即时原子落盘（`.tmp` + `os.replace`）+
> `--resume` 续跑**。景点多时可用 `--only-crowd` 把 210 组降到 42 组。

> **噪声坑（2026-09-17 实测，必须开闸门）**：拥挤度 query 若不设闸门，
> 「于山 人多吗 排队」会召回张家界/莫高窟/九寨沟的通用排队攻略 ——
> 而且因为它们来自新浪/知乎/搜狐这类"权威"域名，**反被标成 A 级**。
> 实测福州行 1677 条里 **56% 被丢弃**（其中 A 级 71 条、U 级 48 条）。
>
> 三道防线：
> 1. **`is_relevant()` 闸门**（默认开启）——异地地名黑名单 + 必须命中景点名或城市名。
>    **顺序是白名单优先**：黑名单里有「烟台」（山东），而福州有「烟台山」，
>    先判黑名单会把「烟台山」整片误杀。
> 2. **`--query-city 福州`** —— 给每条 query 加地名前缀，从源头收窄召回方向。
>    **强烈建议填。**
> 3. **`--regrade`** —— 改了闸门规则后，用已有 `raw.json` 重算分级，
>    **不重复消耗检索额度**。被丢弃的条目另存 `graded_dropped.json` 供复核。

- **产出**：`crowd/graded.json`（带 `platform` / `spot` / `kind` 字段）
  + `graded_dropped.json`（被闸门丢弃的，含 grade，用于评估误杀）
- **提炼**：`digest_crowd.py` 从 `graded.json` 抽出含拥挤/错峰关键词的**句子**
  （按句切分，不是整段摘要）→ `crowd_digest.md`。
  14 景点 169 条 A/U 级 → 78 句，才读得动。

研判结果写入报告的 `CROWD` 字典（逐景点：峰值时段 / 建议到达 / 建议避开 /
排队量级 / 实操建议 / **信源标注**），并配一套**错峰五条总原则**与
**按拥挤度的替换矩阵**（"竹海爆满 → 龙池山自行车公园"）。

> **诚实原则**：`src` 字段必须如实标注情报来自哪些平台。**不编造排队时长**——
> 只写检索到的原话或有出处的量级。季节陷阱（如银杏不在最佳期）也要如实标注。

### 11. 住宿层：住哪儿 + 大概多少钱（`lodging.py`）

**这一层的存在理由**：行程排得再好，没有床位就是废纸。同时它也是**唯一一个
"数据源必须诚实降级"的层** —— 详见下面的接口盘点。

#### 11.1 接口盘点（2026-09-17 实测，别再重复试错）

| 数据源 | 可达性 | 结论 |
|---|---|---|
| **OSM Overpass** | ✅ 免 Key，全球可用 | **唯一"开箱即用"的住宿点位来源**，含名称/类型/星级/房量/官网/电话 |
| **高德 Web 服务 POI** | ✅ 可达，`INVALID_USER_KEY` 说明只是缺 Key | **国内覆盖最好**，含评分与人均；个人自助申请 Key，5 分钟 |
| 百度地图 POI | ✅ 可达，`APP不存在，AK有误` | 同上，需自申请 AK |
| 腾讯位置服务 POI | ✅ 可达，`key格式错误` | 同上，需自申请 Key |
| 天地图（国家平台） | ✅ 可达，参数非法 | 政府数据，需自申请 tk |
| Amadeus Hotel Search | ❌ 连接不通 | 本环境出口不可达 |
| Booking **Demand/XML** | ⚠ 401 Authorization required | **需企业签约**，非开放接口 |
| Booking 官网 / 冰岛页 | ✅ 200 | 网页可达，但无公开数据 API；**抓取违反 ToS，不做** |
| Expedia Partner Solutions | ⚠ 404 / EAN 403 | 需合作方凭据（RapidAPI 镜像同理） |
| 携程 H5 搜索接口 | ⚠ 200 但 `ErrorCode: 4030` | 需有效参数与签名，非开放接口 |
| 携程/飞猪/去哪儿/美团/同程**开放平台** | ✅ 站点可达 | **全部是商户入驻门户**，需企业资质 + 商务签约；无匿名数据接口 |
| TravelPayouts / Hotellook | ⚠ 401 / 404 | 需 Key；Hotellook 免 Key 端点已下架 |
| Hostelworld API | ⚠ 403 | 需 Key |

**一句话结论**：

> **酒店"点位"能免费拿到（Overpass / 地图 POI），酒店"房价"没有任何免费开放接口。**
> 所有 OTA 的价格数据都要企业签约。所以本层**只输出档位区间**，并强制在报告里
> 标注"非实时报价、下单前须复核"。**不要为了让报告好看而伪造精确房价。**

#### 11.2 两个 provider

```bash
# 境外 / 无 Key（默认）—— Overpass，全球通用
python scripts/lodging.py fetch --stays stays.json --out lodging.json

# 国内（覆盖最好）—— 高德，需免费 Key（也可用环境变量 AMAP_KEY）
python scripts/lodging.py fetch --stays stays.json --provider amap \
       --amap-key <你的Key> --out lodging.json

# 出推荐：首选 / 升级 / 特色 / 预算 四档 + 价格档位
python scripts/lodging.py pick --lodging lodging.json --out hotel_plan.json --currency CNY
```

`stays.json` 从 `itinerary.json` 的 `anchors` 里派（`kind == "stay"`）：

```json
[{"town":"绍兴·越城区(鲁迅故里)","lat":29.9930,"lng":120.5820,"radius_km":6,"note":"老城核心"}]
```

#### 11.3 选址规则（`pick` 的核心逻辑）

四档不是随便挑的，每档有明确职责：

| 档 | 规则 | 在报告里的作用 |
|---|---|---|
| **首选** | 住宿锚点 **6 km 内**、优先三星（没有则四星、再退同级酒店）<br>房量 ≥40 间加权，有官网/电话加权（= OSM 上在营且信息完整） | 默认推荐，一家三口最省心 |
| **升级** | 12 km 内的高一档；若当地没有四星，则用**另一家三星**顶上（换环境/带泳池温泉） | 想住舒服一晚时用 |
| **特色** | 名称命中 farm/resort/lodge/basecamp/cottage/温泉… | 农场度假村、木屋、设计酒店 |
| **预算** | 民宿/客栈/青旅，优先近的 | 压总预算用，**须提示确认是否独立卫浴** |

**两个必须处理的坑**：

1. **首选不能只看距离**。小镇里 OSM 常有一条"与镇同名"的泛化条目（如 `Borgarnes`），
   距离最近但毫无信息量 → 必须过滤 `name == town` 的条目，否则首选会是它。
2. **小镇可能真的没有星级酒店**（如冰岛博尔加内斯、国内乡镇）。此时**不要硬凑**：
   回退到 15 km 内的同级住宿，并在 `why` 里如实写"镇内可选不多，最近的是 X km 外的…"。
   撒谎比降级更糟。

#### 11.4 房价档位（内置两套，按币种切换）

**国内（CNY／晚，双人房）**：青旅 80~180｜经济连锁 180~350｜舒适型 350~600｜
高档型 600~1000｜豪华/度假 1000~2500

**境外（ISK／晚，冰岛 7 月旺季，双人房）**：
民宿/客栈 20,000~32,000｜三星 25,000~38,000｜四星 38,000~58,000｜高端/特色 60,000~95,000

依据：2026 冰岛物价指南（三星 17,389~30,266 ISK、四星+ 30,373~94,470 ISK）、
2026 预算指南（旺季 $60~300/晚）、露营卡 €179／家庭·28 晚、营地 €15~25/人·晚。
汇率走 `open.er-api.com`（免 Key，实测可达）。

**家庭房系数 `FAMILY_K = (1.3, 1.6)`**：2 大 1 小需要三人房或加床，冰岛酒店按
"双人房 + 加床"计价，约为双人房价的 1.3~1.6 倍。**报告里的住宿总额必须按整团算，
不能按人均摊**（见 11.6）。

#### 11.5 报告里怎么落位（先生 2026-09-17 指示）

- **费用模块**：加「住宿档位表 + 三/四种住法的 9 晚总价 + 逐晚速查表」，
  另附一个 `<details>` 折叠的**完整候选池**（每镇前 12 家，含距离/星级/房量/档位/参考价）。
- **详细行程**：每天结尾加一行「住宿」，给出**首选 + 升级 + 预算**三家（名称双语、
  星级、房量、距落脚点 km、家庭房价区间）。当天只有一个住宿地时不重复铺开。
- **数据源说明**：在费用模块开头**明写**"无免费开放房价接口，以下为档位区间，
  非实时报价"，并列出信源等级表。**这是硬要求**——境外住宿价格的可信度直接决定
  报告能不能用。

#### 11.6 整团 vs 人均（别把共享成本摊到人头）

| 口径 | 项目 | 为什么 |
|---|---|---|
| **每车固定** | 租车 + 油费 + 隧道/过路 + 停车 | 加到第 4 人也不涨 |
| **整团** | **住宿** | 酒店按"间"计价，2 大 1 小住 1 间家庭房 |
| **每人** | 机票 + 门票/活动 + 餐饮 | 线性增长，儿童常有折扣 |
| 单列 | 手信 | 不并入主区间 |

"再加一个孩子"的边际成本 = 机票 + 门票 + 餐饮 + 加床费，**不是总额 ÷ 3**。

## 可调参数

全部集中在 `scripts/planner.py` / `planner_multi.py` 顶部：

| 常量 | 默认 | 含义 | 调整时机 |
|---|---|---|---|
| `PER_DAY` | 4（单）/ 5（多片区） | 每天最多主景点数 | **一般 ≤6**；亲子游用 `PER_DAY_KIDS`（3~4） |
| `PER_DAY_KIDS` | 3 | 亲子游每日上限 | 带小孩时开 `KIDS_MODE=True` |
| `MAX_DAY_SPAN_KM` | 18.0 / 30.0 | 单日最大地理跨度 | 自驾可放宽到 30~40；纯公交收紧到 12 |
| `FAR_SIGNATURE_KM` | 45.0 | 远郊名片降级阈值 | 愿意跑远途 → 提到 60+；只玩市区 → 降到 25 |
| `SIGNATURE_SCORE` | 5.0 | 招牌点位分数阈值 | 保底范围，一般不用改 |
| `CITY_CENTER` | (120.578, 30.000) | 市中心锚点 | **换城市必须改**，否则距离计算全错 |
| `DAY_MINUTES_BUDGET` | 420 | 满日**净游览**预算（**不含车程**） | 自驾日均车程 80min，勿把它当全天时长 |
| `BIG_SPOT_MIN` | 150 | 大体力点阈值 | 单日 ≥150min 的点最多 2 个 |

**地图侧参数**（`scripts/map_html.py` 顶部）：

| 常量 | 默认 | 含义 | 调整时机 |
|---|---|---|---|
| `DAY_COLORS` | 6 色数组 | 逐日折线配色 | 天数 >6 时需扩充或循环复用 |
| 绕行系数 | 1.35 | 直线距离 → 道路距离 | 平原路网可降到 1.2；山区提到 1.5+ |
| 均速 | 45 km/h | 用于车程估算 | 高速为主可提到 60；城区/山路降到 35 |

> 车程是**量级估算**，不是导航数据。报告与地图里都要写"以导航为准"。

密度公式里的距离系数（`0.28`）和经验值 `PER_DAY=4` 是在绍兴样本上扫描出来的：
- `PER_DAY=3` → 古城占 5 天，招牌全丢
- `PER_DAY=4` → 古城 3 天 + 郊野各归其位（最佳）
- `PER_DAY=5` → 开始出现跨片区硬拼

> **注意**：单目的地模式（绍兴样本）的 `PER_DAY=4` 与多片区自驾的 `PER_DAY=5`
> 是两个独立标定值，不要互相套用。**先生规范是"一般一天不超过 6 个，亲子更少"**——
> 两处都满足。

## 换一个城市要做什么

这个 skill 是**通用流水线**，但有三处城市相关的配置需要替换：

1. **坐标表** `KNOWN_COORDS` —— 目标城市的景点经纬度（GCJ-02）
2. **区域表** `REGIONS` —— 城市的功能分区（老城/新区/近郊/远郊）+ 中心坐标
3. **姓名片** `MUST_SEE` —— 该城市的名片景点清单

前两项可以从 anysearch 检索结果或高德 POI 反查得到。第三项靠常识。

> 目前仓库内建的坐标/区域表是**绍兴专用**。换城市时保留这三个结构、替换内容即可，算法层不需要改动。

## 实测表现（绍兴样本）

输入：上海 → 绍兴，2026-10-01 起 5 天

| 指标 | 结果 |
|---|---|
| 检索条数 | 80 条（A 级 23 / C 级 54 / D 级 3） |
| 抽取景点 | 62 个（34 个高置信 + 28 个中置信） |
| 主行程 | 5 天 / 20 个景点（每天 4 个） |
| 单日最大跨度 | ≤ 15.4 km |
| 门票合计 | 340 元（全程 23 个免费景点） |
| 预算估算 | 3122 元 / 人 |
| 运行耗时 | 约 30 秒（检索占大头） |

天数敏感性验证（同一份景点池，只改 `--days`）：

| 天数 | 主行程景点数 | 变化 |
|---|---|---|
| 3 天 | 12 个 | 只够古城 + 柯桥 |
| 4 天 | 16 个 | 加古城北街区 |
| 5 天 | 20 个 | 加古城核心深度 |
| 6 天 | 21 个 | 五泄自动回归填补 |

单调递增，行为符合预期。

## 已知局限

- **坐标表是手工维护的**。未收录的景点会落到"未定位"，评分降 2.0 分，通常进备选。补坐标即可解决。
- **票价信息可能过时**。规则引擎从检索文本里抽数字，来源若是几年前的攻略就会偏差。报告会标注 C 级信源提醒核对。
- **活动信息最不可靠**。检索往往拿不到当年的节庆排期，报告会明确写"未能获取到确定性活动排期"而不是编造。
- **单目的地模式下不支持多城市联游**，跨城行程需拆分调用。**多片区模式已支持**（见 `references/多片区自驾扩展.md`），但 `DAY_PLAN` 需人工指定每天在哪个片区——算法不会自己推导日程锚点。
- **餐饮建议在单目的地模式下是模板生成的**（`MEAL_TEMPLATES`），按片区套用，不是真实店铺推荐。多片区模式改用 `report_multi.py` 的 `MEALS` 表，可填真实店名。
- **时间轴需手工编排**。`report_drive.py` 的 `TIMELINE` 是逐时人工填写而非算法生成——"几点出发""在哪吃饭"依赖太多外部信息（车程、营业时间、家庭节奏），规则引擎做不到。
- **小红书笔记正文抓不到**。登录墙导致笔记页摘要常为空，只能拿到 URL 和标题。要读正文需另接小红书原生签名直连（需 Cookie，见 TripStar 的 `xhs_service.py` 方案）。
- **U 级信源数量少**。`research.py` 的**城市维度**定向查询能捞到真实笔记页，但命中率仍低（408 条里 8 条），不适合作为主要信源，只做交叉印证。**景点维度**的 `crowd_research.py` 命中率高一个量级（960 条里点评 49 / 小红书 21 / 游侠客 21），是获取拥挤度情报的主力通道，但仍需人工筛噪声。
- **拥挤度只有定性区间，没有实时人流**。给出的"峰值 10:00-14:00""07:30 前抵达"来自攻略交叉印证的量级判断，不是景区实时客流 API。**不编造排队分钟数**——检索到的原话有出处才写，否则只给时段方向。
- **拥挤度数据有季节/年份折扣**。检索到的高峰经验多来自往年国庆/暑期。报告会标注数据年份区间；换季（如淡季出行）时结论需下调。
- **地图需要景点坐标**。`map_html.py` 依赖 `itinerary.json` 的经纬度；未定位的景点不会出现在地图上（也不会进主行程）。
  **2026-09-17 已修**：`planner.py` 现在会把 `lng`/`lat`（GCJ-02）写进每个景点，
  并输出 `coord_sys` 字段；`pipeline.py` 检测到有坐标就自动把内联 SVG 嵌进报告，
  没有坐标则静默跳过（不会留空壳「路线地图」标签页）。
- **地图默认是 SVG 示意图，不是真地图**。没有底图、没有道路几何，连线是**点对点直线**；
  车程按 绕行系数 1.35 / 均速 45 km/h 估算。这是为了「静态交付也能看」而做的取舍。
  要看真地图用 `--map-mode tmap`，但**只在 WorkBuddy 预览面板里有效**。
- **腾讯交互版依赖代理端口**。`__WB_HTTP_PORT__` / `__WB_TMAP_SECRET__` 由 WorkBuddy
  运行时注入。**脱离本环境（发给别人、离线打开）地图必然空白** ——
  这正是 2026-09-17 把默认形态改成 SVG 的原因。交付一律用 SVG。
- **补查询有额度成本**。每轮最多 8 次查询、最多 2 轮，加上初始 17 组，一次完整规划约 **40~55 次调用**。`check` 模式可先离线看缺口再决定补不补。
- **补查询的召回质量不稳定**。缺什么方向不等于搜得到 —— 永泰的「预约限流」补了两轮仍只有 1 条命中。脚本会如实标 `still_gap` 并在报告里写「信息有限」，**不会为了凑数编内容**。
- **天气在超出 15 天窗口后不是预报**。`normal` / `mixed` 模式给的是过往 5 年同期的气候平均，**只能定「带什么衣服」，不能定「哪天下雨」**。报告里逐日标 `≈` 区分。
- **天气只取一个代表点/片区**。`weather.py` 按传入的 `--point` 取数；跨多个气候带的长线行程（如同时含海岛与高山）需多次调用再自己拼。
- **境外轨道已端到端跑通**（2026-09-17 冰岛 9 日环岛自驾实证：2,074 km、
  8 个片区、33 个景点、含住宿与费用）。实测可信链路：
  anysearch 检索 → Photon 核验坐标（修正了 4 处初始偏差）→ OSRM 真实路网测距
  → `planner_multi.py` 显式动线 `order` 编排 → 内联 SVG 地图 → 10 模块报告。
  境外**无中文 UGC**，拥挤度层不适用（改用地理与时间结构判断）。
  本环境下 Wikimedia（含 Wikivoyage）与 Nominatim **被墙**，`vedur.is` / `road.is`
  同样不可达 —— 信源改用 anysearch 检索攻略，路况只能靠检索、不能 API 直连。
- **`report_multi.py` / `report_html.py` / `report_drive.py` 是定制样例**：
  数据（信源表、住宿表、驾驶段、时间轴）写死为「上虞-绍兴-新昌 6 日自驾」那一次的真实内容。
  **当模板用**——复制一份换掉数据表；通用骨架是「读 itinerary.json → 按天渲染 → 附信源附录」。
  它们不再有任何绝对路径硬编码，但**没有配套的 itinerary.json 会友好报错退出**，
  这是设计行为而非故障。
- **境外同名地名可能选错**。Photon 不返回人口字段，`Florence` 会选到美国南卡、
  `Barcelona` 会选到委内瑞拉。已用 `PREFER_KIND` + bbox 面积 + `nest` 缓解
  （Kyoto 已修好），同名地名仍需 `--pick` 手工指定。

## 文件结构

```
trip-plan/
├── SKILL.md                 本文件（给 AI 读的完整手册）
├── README.md                给人读的上手指南（安装 / 迁移 / 排障）
├── requirements.txt         唯一第三方依赖清单（只有 md2html 用得上）
├── 运行.bat                 ★ 单目的地一键生成（双击即用）
├── 快速重排.bat             改天数快速重跑（跳过检索）
├── 生成地图.bat             ★ 出路线图（独立 SVG / 内嵌报告 / 腾讯交互版）
├── 住宿层.bat               ★ 抓住宿点位 + 出推荐
├── 打包.bat                 ★ 生成可搬到别的电脑的 zip（自检清单完整性）
├── 打包.py                  打包逻辑本体（跨平台，命令行直接跑）
├── stays.example.json       住宿层输入样例（住宿锚点格式）
├── scripts/
│   ├── pipeline.py          流水线编排（单目的地，检索→分级→抽取→编排→报告）
│   ├── research.py          城市维度检索 + 信源分级（两种模式共用）
│   ├── gapfill.py           ★ 补查询层（八方向缺口检测 + 相关性闸门 + 最多两轮）
│   ├── weather.py           ★ 天气层（Open-Meteo，预报 / 混合 / 气候平均三模式）
│   ├── crowd_research.py    ★ 景点维度拥挤度/错峰定向检索（并发 + 闸门 + regrade）
│   ├── digest_crowd.py      ★ 拥挤度分句提炼（graded.json → crowd_digest.md）
│   ├── lodging.py           ★ 住宿层（两个 provider：overpass / amap；fetch / pick）
│   ├── extract.py           规则引擎抽取景点（单目的地）
│   ├── planner.py           规则引擎编排行程（单目的地核心算法）
│   ├── report.py            报告生成（单目的地，含拥挤度章 + 折叠信源）
│   ├── places_multicity.py  多片区景点池（通用骨架：改 COORDS/REGIONS/PLACES 即可）
│   ├── planner_multi.py     多片区编排（时间预算 + 片区绑定 + 显式动线 order/cap）
│   ├── report_multi.py      ⚠ 定制样例（上虞-绍兴-新昌）—— 当模板用
│   ├── report_html.py       ⚠ 定制样例（同上，HTML 版）
│   ├── report_drive.py      ⚠ 定制样例（同上，逐时时间轴版）
│   ├── overseas_research.py ★ 境外数据层（Photon + Overpass + OSRM + OpenFreeMap，
│   │                        子命令 geocode / pool / route / all）
│   ├── map_html.py          可视化地图。`--mode svg` 零依赖离线版（默认推荐）；
│   │                        `--mode tmap` 腾讯 GL JS 交互版（仅预览面板有效）
│   └── md2html.py           Markdown → 单文件 HTML（模块化版面 + <details> 折叠 +
│                            --map-mode svg|tmap|none；**唯一需要 markdown 包**）
├── references/
│   ├── 算法设计.md          调度算法的设计推导与参数扫描记录
│   ├── 信源分级规则.md      A/U/C/D 分级判定标准
│   ├── 补查询与质量自检.md  ★ 八方向阈值、三道闸门、与 weekend-city-trip 的对照
│   ├── 境外行程扩展.md      ★ 双轨机制：OpenFreeMap/MapLibre、Photon/Overpass/OSRM、
│   │                        网络出口白名单实测结论
│   ├── 换城市指南.md        移植到新目的地的操作步骤
│   └── 多片区自驾扩展.md    多城市/自驾/日程锚点场景的写法与陷阱
│                            （含 _region_hint 片区归属、篇幅压缩四规范、
│                              拥挤度研判层、地图层与模块化版面）
├── templates/
│   └── 报告模板.md          报告的空白骨架
└── example/
    ├── places.json          绍兴景点池示例（62 条）
    ├── itinerary.json       绍兴 5 日行程示例
    ├── graded_sample.json   信源分级明细示例
    ├── 报告示例.md          完整报告示例（Markdown，含 MODULE 标记与折叠）
    ├── 报告示例.html        完整报告示例（单文件 HTML，含内嵌 SVG 地图）
    └── 路线图_独立SVG.html  ★ 独立路线图示例（零外部依赖，**这就是能发到手机的那个文件**）
```

**打包与搬迁**：任何改动后要给别人用，跑 `打包.py`（或双击 `打包.bat`）。
它会排除 `__pycache__/`、`*.pyc`、`out/` 等临时产物，并在打完**自检**必需文件
（SKILL.md / scripts 全部模块 / references / BAT 入口）是否都在包里 ——
这套 skill 是多文件互相 import 的，手工挑文件必然漏。

## 运行环境（必读，否则第一步就报错）

### 依赖：只有一个

**全部脚本只用 Python 标准库，唯一例外是 `md2html.py`（需要 `markdown` 包）。**

```
requirements.txt →  markdown
```

**Python 3.8+ 即可**，不需要虚拟环境，不需要任何 API Key（anysearch 匿名可用）。

```bash
# 只有需要「Markdown → HTML」时才装
pip install -r requirements.txt
# 或
python -m pip install markdown
```

`md2html.py` 在缺 `markdown` 时会给出可执行的安装提示，不会抛裸 ImportError。

### 本机（WorkBuddy 环境）的两套解释器

本环境下有两个 Python，**分工不同、不要混用**：

| 用途 | 解释器 | 原因 |
|---|---|---|
| 检索 / 补查 / 天气 / 编排 / 报告 / 地图 / 住宿 | `~/.workbuddy/binaries/python/versions/3.13.12/bin/python3` | 无第三方依赖 |
| **仅 `md2html.py`** | `~/.workbuddy/binaries/python/envs/default/bin/python` | venv 里装了 `markdown` |

```bash
export PY_MD=~/.workbuddy/binaries/python/envs/default/bin/python   # 只给 md2html.py 用
export PY=~/.workbuddy/binaries/python/versions/3.13.12/bin/python3
```

坑：用 `$PY` 跑 `md2html.py` 会得到
`缺少依赖：markdown`（旧版是裸的 `ModuleNotFoundError`）。
**`markdown` 已装在 venv `envs/default` 里，不要 `pip install` 到全局。**

**在别的电脑上**：这两个路径不存在是正常的。直接用系统 Python 即可，
只是跑 `md2html.py` 前记得 `pip install markdown`。
`scripts/*.py` 内部**没有任何绝对路径硬编码**（`sys.path` 一律基于 `__file__` 自解析），
所以整个目录复制到任何位置都能直接跑。

## 迁移到别的电脑 / 手机

### 别的电脑（Windows / macOS / Linux 通用）

1. **拷整个 `trip-plan/` 目录过去**（或解压本目录的 zip）。**不要把脚本单独拿出来**——
   `scripts/` 里的脚本互相 `import`，必须整目录在一起。
2. 装 Python 3.8+。
3. **Windows 用户直接双击 BAT**（BAT 自带解释器探测：先找 WorkBuddy 自带、
   再找 PATH 里的 `python`/`py`、最后兜底常见安装路径）：
   - `运行.bat` —— 单目的地一键出报告
   - `生成地图.bat` —— 出路线图
   - `住宿层.bat` —— 住宿点位与推荐
   - `快速重排.bat` —— 改天数重跑
4. **非 Windows 用户**走命令行：

   ```bash
   cd trip-plan
   python scripts/pipeline.py --origin 上海 --city 绍兴           --days 5 --start 2026-10-01 --out ./out
   ```

5. 只有需要「Markdown → HTML」时，先 `pip install -r requirements.txt`。

**要跨平台，优先用「独立 SVG 地图」和「单文件 HTML 报告」两种产物**：
两者都是零外部依赖的纯静态文件，任何系统、任何浏览器打开都一样。

### 手机上

**手机不能跑这套脚本**（要 Python 解释器 + 联网检索）。但**成果可以在手机上看**，
而且这正是本 skill 把地图默认改成内联 SVG 的原因：

| 产物 | 手机上能看吗 | 怎么发到手机 |
|---|---|---|
| **报告 HTML**（`报告_*.html`） | ✅ 单文件、离线可看 | 微信/邮件/网盘直接发，点开即读 |
| **独立 SVG 路线图**（`生成地图.bat` 选项 1） | ✅ 零外部依赖、可双指缩放 | 同上；也能直接打印 |
| `报告_*.md` | ✅ 任意笔记/文档 App | 同上 |
| `map_腾讯交互版.html` | ❌ **手机上必然空白** | 底图走 WorkBuddy 代理，只能在本工具预览面板看 |
| 脚本本身 | ❌ | 需要 Python 运行环境 |

**要点**：给手机 / 给同行家人分享，一律用 **报告 HTML 或独立 SVG 地图**，
不要用腾讯交互版。前者发给谁都能打开，后者换个环境就是白屏。

## 完整工作流（多片区自驾，10 模块报告）

```bash
# 1) 各片区并行检索（城市维度，建景点池）
python scripts/research.py search --city 福州 --start 上海 --out ./ly
python scripts/research.py grade  --raw ./ly --out ./ly

# 2) ★ 补查询：查「现在还开着吗 / 要预约吗 / 最近变了什么」
python scripts/gapfill.py run --graded ./ly/graded.json --city 福州 \
    --year 2026 --month 10 --out ./ly
#    有必补方向仍未解除时再来一轮（最多 2 轮）：
#    python scripts/gapfill.py fill --graded ./yt/graded.json --city 永泰 --alias 福州 --out ./yt

# 3) ★ 天气：逐日天气 + 穿衣带伞建议
python scripts/weather.py --start 2026-10-02 --end 2026-10-05 \
    --point 福州:119.3034,26.0745 --city 福州 --out ./weather_ly.json

# 4) 拥挤度专项（景点维度，逐景点 15 条定向查询）
#    --query-city 必填（不加地名前缀噪声率 56%）；并发 4、逐条落盘
python scripts/crowd_research.py --spots "三坊七巷,鼓山,仙人井,北港村,..." \
    --query-city 福州 --city "福州,平潭,永泰" --workers 4 --out ./crowd
#    改了闸门规则后重算（不重复消耗额度）：
#    python scripts/crowd_research.py --out ./crowd --regrade --city "福州,平潭,永泰"
#    把 A/U 级提炼成可读句子（169 条 → 78 句），再人工写 crowd_notes.json
python scripts/digest_crowd.py

# 5) 建景点池 + 编排（改 places_*.py 的坐标与 MUST_SEE；改 planner_*.py 的 DAY_PLAN）
python scripts/places_multicity.py --out places.json
python scripts/planner_multi.py    --out itinerary.json

# 6) ★ 住宿层：住宿锚点从 itinerary.json 的 anchors 里派（kind == "stay"）
#    stays.json: [{"town":"福州·三坊七巷","lat":26.0745,"lng":119.3034,"radius_km":6}]
#    境外/无 Key 走 Overpass；国内走高德（覆盖最好，含评分与人均，需自申请免费 Key）
$PY scripts/lodging.py fetch --stays stays.json --provider overpass --out lodging.json
#    $PY scripts/lodging.py fetch --stays stays.json --provider amap \
#        --amap-key <你的Key> --out lodging.json
#    出推荐：首选 / 升级 / 特色 / 预算 四档 + 价格档位（--currency CNY|ISK|USD）
$PY scripts/lodging.py pick --lodging lodging.json --out hotel_plan.json --currency CNY
#    ⚠ 价格是档位区间，不是实时报价；报告里必须明写，不许编精确房价

# 7) 出报告（10 模块）+ 单文件 HTML（地图内嵌 SVG，静态也能看）
python report_fuzhou.py --out 报告.md
$PY_MD scripts/md2html.py --md 报告.md --out 报告.html \
    --map-itin itinerary.json --map-station 福州站
#    地图三种形态：
#      默认 --map-mode svg   内联 SVG，零依赖，静态托管/手机/微信都能看（推荐）
#           --map-mode tmap  腾讯交互版，只在 WorkBuddy 预览面板有效
#           --map-mode none  不要地图
#    只想单独出一张路线图（不嵌报告）：
$PY scripts/map_html.py --itin itinerary.json --out 路线图.html --mode svg

# 8) ★ 交付前自检（结构三查必须全过）
$PY - <<'EOF'
import re, json
h = open("报告.html", encoding="utf-8").read()
m = re.search(r"const MODULES = (\[.*?\]);\n", h, re.S)
json.loads(m.group(1))                     # ① 模块 JSON 未被 </script> 提前截断
assert h.count("__WB_HTTP_PORT__") == 0    # ② 无运行时占位符（否则地图空白）
assert h.count("<svg") >= 1                # ③ SVG 已内嵌
print("模块 %d 个 / details %d 个 / svg %d 处" %
      (len(json.loads(m.group(1))), h.count("<details"), h.count("<svg")))
print("OK")
EOF
```

**结构三查之外，报告内容还有「四项自检」**（写在报告末尾的已知局限里）：

| # | 自检项 | 判定 |
|---|---|---|
| 1 | 里程自检 | 无单日超阈值；每日点数 ≤ 上限（亲子 ≤6，多片区用时间预算把关） |
| 2 | 坐标自检 | 全部景点坐标经 Photon/高德核验，偏差过大的已修正 |
| 3 | 时间自检 | 逐日「游玩 + 车程」在预算内，长驾日在正文说明 |
| 4 | **成本口径自检** | **境外自驾是否已把「里程税」单列**（见上面「油费口径已变」小节）；沿旧油价又漏里程税会少算约 ¥800/车 |


## 与其他方案的关系

本 skill 是"方案 A"——**纯规则引擎 + 零 LLM 依赖**。

优势：离线、免费、可复现、快。
代价：抽取和编排靠规则，遇到检索文本格式异常时需要补规则。

如果需要"让模型自己判断"的灵活性（例如处理非标准表述、动态调整偏好权重），可以在抽取层和编排层之间插入一个可选的大模型环节。`extract.py` 的输出（`places.json`）和大模型的输入格式是一致的，所以这个替换是局部的、不影响其他环节。

> 设计取舍：这个 skill 故意不依赖大模型，因为**行程规划的瓶颈是数据质量和地理约束，不是语言理解**。规则引擎在这两件事上比模型更稳定。
